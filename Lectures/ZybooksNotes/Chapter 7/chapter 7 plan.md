Create a slide deck covering while loops for an introduction to programming course.
- Describe how while loops work generally. Compare them to if statements and that they have the same structure, but that while loops return back to the top to reevaluate the boolean expression.
- Create a slide with a simple example that is different than the ones in the notes. Have a markdown text editor describing the algotithm and a codee block to run it. Define iteration whereever it make sense.
- Have another example iterating over a list using a while loop to find the largest item in the list.
- Have a slide describing what a sentinel value is and an accompany code example that is different than the ones in the notes.
- Describe infinite iteration, indefinite iteration, and finite iteration. 
- Have a slide on the GCD example from the notes. Include a counter variable on this slide and define what a counter variable is.
- Somewhere discuss the difference between a sentiel variable and a loop variable.